// import dependencies
import React, { Component } from "recat";

// What good is website without modals?
class Modal extends Component {
  constructor(props) {

  }

  render() {
    return(
      <section>
        Why doesn't it auto populate?
      </section>
    );
  }
}

export default Modal;
